#include "main.h"

/**
 * main - Prints all possible combinations of single-digit numbers.
 *
 * Return: Always 0.
 */

void print_alphabet(void)
{
char a;
for (a ='a' ; a <= 'z'; a++)

_putchar(a);

_putchar('\n');
}
