#include "main.h"
/**
 * _isdigit - Prints every minute of the day of
 *              Jack Bauer, starting from 00:00 to 23:59.
 */
int _isdigit(int c)
{
if (c >= 0 && c <= 9)
{
return (1);
}
else
{
return (0);
}
}
