#include "main.h"
/**
 * _isupper - Prints every minute of the day of
 *              Jack Bauer, starting from 00:00 to 23:59.
 */
int _isupper(int c)
{
if (c >= 65 && c <= 90)
return (1);
else
return (0);
}
